model Evento {
  id        String   @id @default(cuid())
  nome      String
  data      DateTime
  cidade    String
  imagemUrl String
  link      String?
  ativo     Boolean  @default(true)
  destaque  Boolean  @default(false)
}
